@using Microsoft.AspNetCore.Identity

@inject SignInManager<AppUser> SignInManager
@inject UserManager<AppUser> UserManager

@if (SignInManager.IsSignedIn(User))
{
    <div class="btn-group">
        <form asp-area="Identity" asp-controller="Account" asp-action="LogOff" method="post" id="logoutForm" class="navbar-right">
            @*<ul class="nav navbar-nav navbar-right">
                <li class="nav-item">
                    <a class="nav-link text-light" asp-area="Identity" asp-controller="Manage" asp-action="Index" title="Manage"><i class="fas fa-user"></i> @UserManager.GetUserName(User)!</a>
                </li>
                <li class="nav-item">
                    <button title="Đăng xuất" type="submit" class="btn btn-link navbar-btn navbar-link text-light">
                        <i class="fas fa-sign-out-alt"></i>
                    </button>
                </li>
            </ul>*@
                <button type="button" class="btn btn-sm btn-light dropdown-toggle" data-toggle="dropdown"><i class="fas fa-user"></i> @UserManager.GetUserName(User)</button>
                <div class="dropdown-menu dropdown-menu-right">
                    <button asp-area="Identity" asp-controller="Manage" asp-action="Index" title="Manage" class="dropdown-item">Information</button>
                    <button class="dropdown-item" type="submit">Sign Out</button>
                </div>
        </form>
    </div>
}
else
{
    @*<ul class="nav navbar-nav navbar-right">
        <li class="nav-item">
            <a title="Đăng nhập" class="nav-link text-light" asp-area="Identity" asp-controller="Account" asp-action="Login">
                <i class="fas fa-sign-in-alt"></i>
            </a>
        </li>
    </ul>*@
    <div class="btn-group">
        <a class="btn btn-sm btn-light" asp-area="Identity" asp-controller="Account" asp-action="Login">Sign In</a>
    </div>
}
